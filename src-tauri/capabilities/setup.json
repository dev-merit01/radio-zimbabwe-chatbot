{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "setup",
  "description": "Only the bundled connection screen may read settings and connect.",
  "windows": ["setup"],
  "local": true,
  "permissions": ["allow-get-configuration", "allow-connect-server"]
}
